# Iterations index

Delivery compasses and iteration-scoped planning snapshots (optional). Link from `plans[].metadata` via `iteration_compass` or `iteration_refs` when used.

| Document | Version / iteration | Description |
|----------|---------------------|-------------|
